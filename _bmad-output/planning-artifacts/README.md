# Archon BMad Planning Handoff

This folder is the active local planning input package for Archon implementation of Hermes Agent Workflow Commander.
The downstream implementation root is `/Users/dale/Desktop/workspace/OceanLabs/workflow-engine/Archon`.
Run Archon implementation workflows from that root.

Implementation agents must use these flat files as the local source of product, architecture, and story context:

```text
prd.md
architecture.md
epics.md
contracts/workflow-commander/README.md
```

These files contain the Archon-owned slice of the current headless Workflow Commander plan.
They are written here so Archon agents can work locally without reading parent planning documents.
Archived folders and feature-scoped subfolders are not active implementation input unless an Archon BMad workflow is explicitly changed to read them.

The Archon-owned scope includes Workflow Provider Binding, generic `provider` and `name` controller vocabulary, CLI JSON producer contracts, workflow run state, retry, resume, cancel behavior, workflow event production, non-blocking event outbox, delivery status, signed workflow event production, and Archon-side validation guidance.
The Archon-owned scope excludes Project Binding, BMAD mount, cwd enforcement from Hermes, BMAD invocation from Hermes, materialization, Project Work Items, Phase Tasks, HILT Gates, workflow event ingress, Story Status History, reconciliation, diagnostics, and Hermes user interaction.

The local contract package placeholder is `contracts/workflow-commander/README.md`.
That placeholder documents required schema and fixture families, but it does not satisfy contract-readiness gates.
Producer stories remain blocked until the needed schemas and JSON examples exist locally or are regenerated into this handoff package.

Do not place Archon implementation artifacts in this planning folder.
Archon implementation artifacts are generated by BMad workflows inside the Archon project.

Recommended downstream validation command from `/Users/dale/Desktop/workspace/OceanLabs/workflow-engine/Archon`:

```text
bun run validate
```
