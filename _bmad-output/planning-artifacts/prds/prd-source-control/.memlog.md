---
topic: Archon Source Control tab (workflow-run screen): read-only remote git inspection of a run's checkout
updated: 2026-08-31T19:57
---

- (decision) Create PRD for Archon Source Control feature; input = finalized spec-archon-source-control (SPEC.md + brownfield/viewer-rules/architecture-diagrams/roadmap) + brainstorm-intent; Fast path; English
- (decision) Output location = Archon subproject planning-artifacts, feature-scoped subfolder prds/prd-source-control/ to avoid clobbering existing Hermes Workflow Commander handoff prd.md; honors cross-project handoff contract (isolated Archon package)
- (decision) Access: any user who can view the run (open admin/member, single-tenant multi-user); no new per-tab restriction
- (decision) Success metric: operators inspect a run's changed files/diffs on the UI without SSH/clone (adoption + task completion); counter-metrics: no run-screen perf regression, no secret-exposure incidents; no hard numeric targets (internal)
- (decision) v1 scope = CAP-1..7 (MUST, M/A/D); CAP-8 durable snapshot = fast-follow; container + COULD deferred
- (decision) Latency: fetch-on-click with explicit loading; no hard SLA; large files stream per viewer-rules
- (decision) Rollout: ship without a feature flag (read-only, low risk)
- (decision) Secrets (failure-1): v1 DEFERS redaction/denylist and records the residual risk (reading arbitrary worktree files may surface .env/keys in the UI); acceptable given read-only + trusted internal users; revisit if exposure surfaces
- (decision) Handoff-contract compliance: prd.md + addendum.md must be self-contained (isolated Archon agent, no parent traversal); inline load-bearing spec/companion content instead of referencing parent spec paths
- (event) Fast-path draft written: prd.md (7 features, FR-1..9, CAP-8->FR-9 fast-follow) + self-contained addendum.md (endpoints, viewer rules, thresholds, snapshot format, container exception, lifecycle, code refs, diagrams). Pending user review before finalize
- (event) Reconcile PASS (self-contained, no parent dependency). Fixes applied: G1 CAP-8 trigger contradiction -> aligned to run-end-decided (prd FR-9 note/§8.2 + addendum); G2 container->empty-state added to FR-8 consequences; G3/G4 acceptable. Report: reconcile-source-control.md
- (event) Rubric review PASS (dimensions strong). Fixes: FR-1 ref FR-7->FR-8; Glossary ref FR-8->FR-9; assumptions index +Observability +Rollout; HOW (execFileAsync/@archon/git/registerOpenApiRoute) softened out of PRD body to addendum refs; SM-1 measurement instrument added. Report: review-source-control.md
- (event) Triage: 4 open questions all NON-blocking (existence=impl validation task; FR-9 snapshot format=fast-follow; thresholds=recorded defaults; secret=deferred with recorded residual risk). None block architecture/epics; deferred to implementation with revisit conditions logged
- (event) Polish: proportionate structural+prose pass applied inline (reviewer graded structure/prose strong; docs lean+consistent M/A/D). Formal bmad-editorial-review-structure/prose available on request. External handoffs: none configured
- (event) PRD finalized
- (change) Diagram fix: resolution flow now branches container-backend -> Empty state BEFORE realpath/git, so a git-backed container run cannot bypass FR-8 and read stale host state; removed leftover working-title line from finalized PRD
- (change) Diagram fix confirmed: container-backend -> Empty state branch before realpath/git; working-title line removed
