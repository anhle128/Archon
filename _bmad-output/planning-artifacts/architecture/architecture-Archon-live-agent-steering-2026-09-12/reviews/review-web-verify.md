# Review — Web-Verify / Reality-Check lens

Spine: `ARCHITECTURE-SPINE.md` (Live Agent Steering, 2026-09-12, updated 2026-09-13)
Companions skimmed: `provider-steering-matrix.md`, `engine-integration.md`
Lens: verify every committed decision was web-researched or source-read, not asserted from training data.

**Verdict:** All load-bearing repo anchors hold (line numbers drifted 0–2 lines, no claim broke). All web/version claims hold with ONE minor currency drift: codex-sdk npm "latest" is now **0.154.0**, not the cited 0.153.4. The SDK behavioral reasoning (interrupt + streaming at the pin, uuid echo deferred) is confirmed against the real published type declarations.

Method: repo-reality checked by reading the actual source; version pins read from `package.json` + `bun.lock`; SDK behavior read from the published `.d.ts` on npm/unpkg for the exact versions; `AbortSignal.any` tested in the live Bun runtime.

---

## A. Repo-reality anchors (source-read — the load-bearing ones)

| Claim                                                                                              | Cited                                  | Actual                                                                                                                                     | Verified?    | Note                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `nodeAbortController = new AbortController()` created once per node                                | `dag-executor.ts:2209`                 | `:2209`                                                                                                                                    | source-read  | EXACT. Comment: "Create per-node abort controller for idle timeout cleanup"                                                                                                                                                  |
| `abortSignal: nodeAbortController.signal` passed to sendQuery options                              | `:2221`                                | `:2223`                                                                                                                                    | source-read  | Drift +2. Claim holds — `nodeOptionsWithAbort: SendQueryOptions = { ...nodeOptions, abortSignal: nodeAbortController.signal }`                                                                                               |
| Re-ask re-invokes `sendQuery(..., attemptResumeId, ...)` on same session                           | `:2290`                                | `:2290`                                                                                                                                    | source-read  | EXACT. Inside `runStreamPass(attemptPrompt, attemptResumeId, ...)`: `aiClient.sendQuery(attemptPrompt, cwd, attemptResumeId, passOptions)`. The session-resume seam the spine reuses is real                                 |
| Re-ask loop guard includes `!nodeAbortController.signal.aborted`                                   | `:3031`                                | `:3032`                                                                                                                                    | source-read  | Drift +1. `reaskAttempt < maxReasks && !nodeIdleTimedOut && !nodeAbortController.signal.aborted`                                                                                                                             |
| `if (nodeAbortController.signal.aborted && !nodeIdleTimedOut)` → node_failed / 'Cancelled by user' | `:3123`                                | `:3124`                                                                                                                                    | source-read  | Drift +1. Emits `node_failed`, `recordFailedStatus('Cancelled by user')`, returns `{state:'failed', error:'Cancelled by user'}`. Does not throw                                                                              |
| Idle-timeout path COMPLETES the node ("completed via idle timeout")                                | `:3118`                                | `:3118` (message), block `:3110–3121`                                                                                                      | source-read  | EXACT. `dag_node_completed_via_idle_timeout` at `:3113`; the `!nodeIdleTimedOut` guard at `:3124` keeps idle-timeout out of the fail branch. **Nuance:** it completes only when output exists (`nodeOutputText.trim() !== '' |     | structuredOutput !== undefined`); a zero-output idle-timeout with `output_format`throws at`:3098–3106`. The spine's warning that reusing this path would "ship the partial turn as output" is accurate for the output-present case |
| Idle-timeout callback + cancel-poll call `nodeAbortController.abort()`                             | `:2298` / `:2325`                      | `:2298` / `:2325`                                                                                                                          | source-read  | EXACT. `:2298` inside `withIdleTimeout` callback; `:2325` inside the 10s cancel/pause poll                                                                                                                                   |
| Catch-block cancel classifier ("never reaches the catch-block classifier at :3386")                | `:3386`                                | `:3387`                                                                                                                                    | source-read  | Drift +1. `if (nodeAbortController.signal.aborted && !nodeIdleTimedOut && !(error instanceof AskHuman...))` → classify as cancel                                                                                             |
| `shouldContinueStreamingForStatus` returns true for `running` AND `paused`                         | `:700–702`                             | `:700–702`                                                                                                                                 | source-read  | EXACT. `return status === 'running'                                                                                                                                                                                          |     | status === 'paused';`                                                                                                                                                                                                              |
| `CANCEL_CHECK_INTERVAL_MS = 10_000` (10s)                                                          | `:682`                                 | `:682`                                                                                                                                     | source-read  | EXACT                                                                                                                                                                                                                        |
| `codex.resumeThread(resumeSessionId)` exists + used, with `startThread` fallback                   | `codex/provider.ts:1006`               | `:1006`                                                                                                                                    | source-read  | EXACT. `thread = codex.resumeThread(resumeSessionId, threadOptions)` in try; `catch → codex.startThread(threadOptions)` fallback at `:1010`                                                                                  |
| Codex calls `thread.runStreamed(prompt, turnOptions)` (one-shot form)                              | `codex/provider.ts:1093`               | `:1093`                                                                                                                                    | source-read  | EXACT. `const result = await thread.runStreamed(effectivePrompt, turnOptions)`                                                                                                                                               |
| `workflow_node_messages` `kind` = `text \| tool \| status`, no speaker field                       | `node-message.ts:39–43`                | discriminatedUnion `:30/:33/:36`, union `:39`                                                                                              | source-read  | EXACT. Three `z.literal('text'/'tool'/'status')` arms; `origin` correctly absent today                                                                                                                                       |
| `nodeTranscriptMetadataSchema` is `.strict()`; `origin` is an additive field to add                | `node-execution.ts:29–43`              | `:29–43`                                                                                                                                   | source-read  | EXACT. `.strict()` at `:43`; `origin` not present yet (spine frames it as the additive change). Bonus: `outcome` enum at `:40` already includes `'interrupted'` — the glyph the companion references exists                  |
| `sendQuery`'s options type accepts `abortSignal`                                                   | sanity-check                           | `providers/src/types.ts:579` (`abortSignal?: AbortSignal` on `AgentRequestOptions`), `:718` `SendQueryOptions extends AgentRequestOptions` | source-read  | CONFIRMED. Field is typed; `dag-executor.ts:2221` typechecks against it                                                                                                                                                      |
| `AbortSignal.any` is available in the Bun runtime                                                  | sanity-check                           | Bun **1.3.14**: `typeof AbortSignal.any === 'function'`, constructs a real `AbortSignal`                                                   | runtime-test | CONFIRMED live. The spine's `AbortSignal.any([nodeSignal, perTurnSignal])` design is runnable here                                                                                                                           |
| Web dispatch runs the executor in the API server process                                           | `api.ts:3200–3228`                     | `:3200` `await Promise.all([import('@archon/workflows/executor'), ...])`, `executeWorkflow` awaited `:3208`                                | source-read  | EXACT. Confirms "in-process registry is reachable for web dispatch"                                                                                                                                                          |
| CLI `--detach` = detached child in its own process group                                           | `cli/src/commands/workflow.ts:696–722` | `:703–709` `spawn(cmd[0], ..., { detached: true, windowsHide: true })`, pid check `:721`                                                   | source-read  | EXACT. Confirms "no reachable live handle across the process boundary" (AD-5)                                                                                                                                                |
| Console-import boundary is the `no-restricted-imports` lint rule                                   | `eslint.config.mjs:126–163`            | `no-restricted-imports` console block ≈ `:127–160` (file is 164 lines)                                                                     | source-read  | CONFIRMED. Blocks console-spike imports of prod web modules + runtime `@/lib/api` + react-query                                                                                                                              |

**A-summary:** 19/19 anchors verified by reading source. Line-number drift is 0–2 lines everywhere; every CLAIM at/near each anchor still says what the spine/companions say. No anchor moved enough to break a claim.

---

## B. Web / version claims

| Claim                                                                                                                                                            | Verified?                | Note                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@anthropic-ai/claude-agent-sdk` pinned **0.3.209**                                                                                                              | source-read + web        | `package.json` (root + providers) and `bun.lock` all pin `0.3.209`. npm has it (published 2026-07-14). npm dist-tag `latest` is now **0.3.270** (2026-09-12) — the pin is ~2 months / 61 releases old, but this is a deliberate inherited pin and the Stack table is explicitly "Seed — verified at authoring." Not a defect                                                                                                    |
| `interrupt()` present at the pin (≥ 0.3.205)                                                                                                                     | web                      | `sdk.d.ts@0.3.209` exposes `interrupt(): Promise<SDKControlInterruptResponse \| undefined>`. Present at the pin — no upgrade needed for CAP-2. CONFIRMED                                                                                                                                                                                                                                                                        |
| AsyncIterable streaming input present at the pin (introduced ≤ 0.3.142)                                                                                          | web                      | `sdk.d.ts@0.3.209` exposes `streamInput(stream: AsyncIterable<SDKUserMessage>): Promise<void>` and the `Query` extends `AsyncGenerator`. Streaming-input transport is at the pin — no upgrade needed for CAP-5 soft-inject transport. CONFIRMED                                                                                                                                                                                 |
| `user_message_uuid` echo NOT at the pin; needs ≥ 0.3.246                                                                                                         | web                      | `0.3.209` has **no caller-set uuid echo** on `SDKUserMessage` — only a system-generated `prompt_id` correlation id. `0.3.246` exists on npm and `0.3.209 < 0.3.246`, so CAP-6 `delivered` is genuinely unreachable at the pin. The spine's CAP-6 deferral (AD-8) is correctly grounded. CONFIRMED                                                                                                                               |
| `@openai/codex-sdk` `^0.144.5`, lockfile pins **0.144.5**                                                                                                        | source-read              | `packages/providers/package.json` = `^0.144.5`; `bun.lock` = `0.144.5`. Caret has not floated. CONFIRMED                                                                                                                                                                                                                                                                                                                        |
| Codex TS SDK exposes only `run()`/`runStreamed()` — no `turn()`/`steer()`                                                                                        | web + source             | `index.d.ts` at **0.144.5** (pinned) AND **0.154.0** (current latest): `Thread` has only `run(input, turnOptions?)` and `runStreamed(input, turnOptions?)`; `Codex` has `startThread()`/`resumeThread()`; cancellation is `TurnOptions.signal?: AbortSignal` (= the spine's "stream-abort"). No `turn()`/`steer()` at either version. CONFIRMED — codex steering = stream-abort + re-run on resumed thread, as the spine states |
| "npm latest [codex-sdk] cited as 0.153.4"                                                                                                                        | web — **DRIFTED**        | npm dist-tag `latest` is now **0.154.0** (published 2026-09-09); `0.153.4` exists but is superseded. Also an `alpha` tag at `0.155.0-alpha.3.10`. **Minor** — not load-bearing: the "only run()/runStreamed()" fact holds at 0.144.5, 0.153.4 AND 0.154.0, so no design consequence. Update the citation for accuracy                                                                                                           |
| Five providers named (claude, codex, omp, grok, deepseek) exist and fit                                                                                          | source-read              | All five have adapter dirs: `claude/`, `codex/`, `grok/` (built-in, registered in `registry.ts` `:120/:137/:156`), `community/omp/`, `community/deepseek/`. "Each named technology still exists" holds                                                                                                                                                                                                                          |
| Per-provider mechanism specifics (omp `steer`/ACP-cancel trap; deepseek 31 ms cancel-and-continue, 2 ms concurrent-prompt reject; grok leader socket / `-32601`) | could-not (this session) | External-runtime research, not re-runnable from repo+web here. The matrix labels each with its evidence basis ("Source", "Measured", "Advertised + measured") rather than presenting them as bare assertions — an honest evidence trail. Nothing reads as asserted-not-checked; deeper re-verification of the external runtime timings is out of scope for this lens                                                            |

**B-summary:** Every version pin is real and read from the repo; every claude/codex SDK behavioral claim is confirmed against the exact published `.d.ts`. One stale currency citation (codex latest 0.153.4 → now 0.154.0). The claude pin being 61 releases behind `latest` is by design, not an error — the feature needs no upgrade.

---

## Findings that could be updated (none block the spine)

1. **Codex-sdk "npm latest = 0.153.4" is stale** — latest is now `0.154.0` (2026-09-09). `provider-steering-matrix.md:49` and `ARCHITECTURE-SPINE.md:196`. Non-load-bearing (the run()/runStreamed()-only fact holds at 0.154.0 too). Fix the citation.
2. **Line-number drift on 4 anchors** (all +1/+2): abortSignal `:2221`→`:2223`, re-ask guard `:3031`→`:3032`, node-fail `:3123`→`:3124`, catch classifier `:3386`→`:3387`. No claim broke. Per the repo's own convention (stable text anchors over raw line numbers), these will keep drifting — the claims themselves are solid.
3. **Claude pin currency** — `0.3.209` is ~2 months / 61 releases behind `latest` `0.3.270`. Deliberate inherited pin; the spine's own "Seed — verified at authoring; the code owns it once it exists" framing covers this. No action required beyond awareness that a future pin bump past `0.3.246` flips CAP-6 from Deferred to buildable.

## Could-not-verify (flagged honestly, out of lens scope)

- External-runtime provider timings/behaviors for omp, deepseek, grok (steering-mode defaults, cancel latencies, leader-socket reachability). These are marked in the matrix with their evidence class and were not independently re-run this session.
