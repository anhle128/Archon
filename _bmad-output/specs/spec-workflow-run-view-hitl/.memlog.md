---
topic: Workflow run view HITL: mid-turn AskHuman + node-centric run view on legacy + console
slug: workflow-run-view-hitl
updated: 2026-09-05T22:03
---

- (event) CREATE: original spec folder missing on disk; restore from architecture memlog + finalized ARCHITECTURE-SPINE.md; preserve CAP-1..7 IDs from coaching/UX map
- (direction) Adopt architecture spine as companion (AD-1..AD-9 binding HOW). Kernel holds WHAT. User ratified CAP-7 narrowing: fail-loud only when allowed_tools names AskHuman on askHuman:false
- (capability) CAP-1 Graph + Logs: operator inspects a run as a node-centric graph AND an unmerged node-run history; both are entry points into one node room per surface
- (capability) CAP-2 Per-type node panel: operator opens a panel matching node type (agent room for command/prompt/loop; stdout for bash/script; declared gate as itself; child-run link for workflow; controller/container for route_loop/loop_group)
- (capability) CAP-3 Chat timeline + Ask inline: operator follows user turns plus node-status entries; Ask cards render inside the agent room at the moment of the tool invocation
- (capability) CAP-4 Ask contract: an agent node can pose structured questions mid-turn; the starter submits one answer set or declines; Other is valid only with free text; Submit stays disabled until every question is valid
- (capability) CAP-5 Concurrency: N asks may be pending at once; a node stays awaiting until all of its asks resolve; run-level awaiting clears when the last pending interaction in the run resolves
- (capability) CAP-6 Starter-only: only the identity that started the run can answer; teammates see a factual waiting note without Submit/Decline
- (capability) CAP-7 Unsupported provider: a run that names AskHuman in allowed_tools on a provider without an in-process ask channel is rejected at start, fail loud; other runs on those providers proceed without the tool
- (constraint) Build BOTH legacy WorkflowExecution and /console; engine+contract surface-agnostic; fronts are thin renderers of one envelope/states/copy — not a shared React NodePanel
- (constraint) AskHuman tool invocation is the ONLY ask channel; never prose-sniff; residual prose-ask accepted; do not wrap Claude AskUserQuestion; console Reply is not HITL
- (constraint) No workflow YAML authoring-language changes
- (constraint) ONLY run starter may answer; missing user_id fails when an Ask would be persisted, not at every identity-less CLI start; first answer wins; already-resolved is 409
- (constraint) Blocked node waits indefinitely — no timeout, no auto-default, no autonomous lifecycle mutation of non-terminal work
- (constraint) Ask form built fully; Permission v1 is envelope/type-contract only (kind permission, call_id equals tool_use_id, confirm body intent); variant cards deferred until a live activation source
- (constraint) Keep Logs/log-history on both surfaces; graph is an additional per-node entry; awaiting chrome is warning/waiting-on-you never error; CAP-7 start reject is the error state
- (constraint) Architecture ADs AD-1..AD-9 in the adopted spine are binding implementation invariants (pending table, pause without approval slot, transcript table, shared run-graph, AskHuman ownership, resume injection, REST/SSE, additive schema, ask channel)
- (constraint) Additive-only schema both dialects; awaiting is not a new run status; declared gates keep ApprovalContext; AskHuman never rides metadata.approval
- (constraint) Console isolation stands except the one sanctioned packages/web/src/lib/run-graph/ pure-TS import; type-only api.generated.d.ts allowed
- (assumption) Original SPEC.md/hitl-contract.md/ux-design.md were not on disk at this derive; CAP intents restored from architecture coaching CAP map and UX mockup session, not a recovered file
- (assumption) v1 Ask providers are Claude and Pi only (capabilities.askHuman)
- (note) Non-goals: Permission live cards; per-node independent scheduling; CLI/chat/manage_run answer UX; general cyclic graphs; new deploy topology; source-control viewer inside the node room; changing NativeTool.handler return type
- (event) kernel distilled; companions: hitl-contract.md (wire), brownfield.md (today's seams), adopted spine + project-context.md
- (event) Self-validate pass 1 coherence: CAP-1..7 each have intent+success; intents are WHAT; constraints rule things out; non-goals explicit; success signal demonstrable; IDs stable CAP-1..7 never reused
- (event) Self-validate pass 2 preservation: dual-surface, Ask-only channel, no YAML, starter-only, wait-forever, Ask-full/Permission-envelope, Logs retained, additive schema, console isolation, AD-1..9 HOW, CAP-7 allowed_tools narrowing, deferred spikes all landed in SPEC.md or companions; diagrams remain in adopted spine (not duplicated); original spec files were absent so restoration is logged as assumption
