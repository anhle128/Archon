---
topic: Archon UI file and git views on the workflow-run screen
goal: Inspect the remote worktree from the workflow-run screen: open a file, see git-changed files, see git history, without a local clone
mode: partner
status: complete
updated: 2026-08-30T21:06
---

- (decision by user) Accepted Creative Partner, four techniques (Job to Be Done, Morphological Analysis, Constraint Mapping, SCAMPER), and the remote worktree inspection goal
- (direction by user) Use Vietnamese for the session
- (technique) started Job to Be Done
- (idea by user) Job: check file status, which files have currently changed, and what their content is
- (idea by coach) The hire is a live remote worktree radar: dirty status + changed paths + file content, not a generic IDE file tree; answer whether this node touched what you think it touched
- (direction by user) Reference VS Code Explorer git decorations and Source Control: need file status, current changes, content, and which files previous commits included
- (idea by user) Need to know which files the previous commit already committed
- (idea by coach) The hire is time-travel on the remote worktree: now (dirty status+content) versus a past commit's file list, VS Code SCM as the reference shape not a local clone
- (idea by user) Click any commit on the graph like VS Code, not only HEAD or this run's commits
- (idea by coach) The graph is the index into remote history; after click, inspect that commit's file set as a time-travel target
- (idea by user) No diff needed; want file content at that commit snapshot
- (idea by coach) Two clocks one reader: live remote worktree content for dirty files, and read-only blob at a clicked commit; not a diff tool
- (idea by user) Or view like VS Code side-by-side: see what was added to that file, red/green highlighted content
- (idea by coach) Not a diff tool to operate; a reader with additions lit up so you can see what landed in the file
- (idea by user) Red is before the change, green is after the change
- (idea by coach) One reader, two modes: Diff (red before / green after) and Snapshot; for a clicked commit Diff is that commit vs parent, for dirty files Diff is worktree vs HEAD
- (decision by user) Diff mode only for now; Snapshot not supported
- (idea by coach) v1 is a change-reader not a file-reader: open only paths that have a before/after (dirty vs HEAD or commit vs parent)
- (question by user) Did not understand the clean-file Explorer question
- (technique) started Morphological Analysis
- (idea by coach) Morphology axes: placement on run screen, which file list, which clock, Diff-only viewer, remote worktree of this run
- (idea by user) In a commit file list, two states only: M modified and A append/new file
- (idea by coach) Status axis M vs A: M is red-before green-after vs parent; A has no before so Diff is green-only after
- (idea by user) A is Added: no left/right panes, no red/green, just show file content like snapshot
- (idea by coach) Viewer keyed by status: M opens Diff vs parent, A=Added opens single-pane snapshot of the new file
- (idea by user) Now untracked new files also use single-pane no-color viewer like commit Added
- (idea by coach) Same viewer rule on both clocks: M opens Diff, A/untracked opens single-pane content; Now diffs vs HEAD, commit diffs vs parent
- (idea by user) Untracked is not a third type: Now files also show only M and A, same mechanism as files inside a commit
- (idea by coach) One list widget for both clocks: M and A only; Now is worktree vs HEAD, History is commit vs parent; no separate untracked viewer
- (question by user) Did not understand the Now-as-Changes-block-vs-unclicked-commit question
- (idea by user) Both regions at once like VS Code: uncommitted Changes and commit graph
- (idea by coach) Layout: Changes M/A chua commit fixed above commit history; click a file in either region opens the same viewer
- (idea by user) Confirmed VS Code-like layout: Changes above commit history, same viewer from either region
- (idea by coach) Do not name git history Graph: Archon already has workflow Graph tab; put Changes+history+viewer in a new run tab such as Source Control
- (idea by user) Ok: fourth tab Source Control beside Graph Logs Chat
- (technique) started Constraint Mapping
- (idea by coach) Real constraint: laptop has no repo so Source Control must read this run's remote worktree; turn it into the feature not a limitation
- (idea by user) Slow is the painful remote constraint
- (idea by coach) Attack slow: fetch on click only, show loading and error, keep viewer still; refresh is a button plus file-changed notice not a jumping live view
- (decision by user) Manual Reload button, not auto-update
- (idea by coach) Manual reload also caps remote load: list and file fetched only on demand, no polling of the remote worktree
- (direction by user) Must find a way to open large files, not block them
- (idea by coach) Open big files by streaming chunks: load first screenful, Load more on scroll, fetch only visible range
- (idea by coach) Diff-only payload: server sends just changed hunks with a few context lines, not the whole file, so big M files stay small
- (idea by coach) Skeleton then fill: show file list and sizes instantly from metadata, viewer paints a skeleton while bytes arrive, cancelable
- (idea by coach) Binary is not text-diffed: image renders inline, pdf/binary offers download or hex-peek, never dumped as garbage text
- (direction by user) Delegates the latency method to coach; only requirement is files must open
- (decision by coach) Chosen open method: M sends diff hunks plus context, A/large text streams in chunks with Load more, metadata-first skeleton with Cancel, binary renders image or offers download/hex; nothing blocks, everything opens
- (insight by coach) Design is largely settled; user is delegating and terse, a natural point to converge or wrap
- (technique) started SCAMPER
- (idea by coach) Combine Logs+Source Control: a log line that ran a command links to the files that command touched, jump straight to their diffs
- (idea by coach) Adapt the workflow Graph: each node shows a changed-files badge; click a node to filter Source Control to files that node produced
- (idea by coach) Put-to-other-use: reuse the diff viewer as the HITL review surface so an approval gate shows exactly the run's diffs before approve
- (idea by coach) Reverse the default: open Source Control on this run's own commits first, full history is the fallback filter
- (idea by coach) Eliminate empty Changes: on auto-commit no-HITL runs there is nothing uncommitted, so collapse Changes and lead with history
- (decision by user) All five run-aware SCAMPER ideas kept, none wasteful
- (insight by coach) SCAMPER batch spent; four-technique batch complete with a settled design plus five run-aware differentiators
- (decision by user) Chose path 1: run another idea batch
- (technique) started Failure Analysis
- (idea by coach) Failure: diff viewer of remote files could expose .env/secrets/keys living in the worktree; needs redaction or a denylist before showing content
- (idea by coach) Failure: this is a multi-repo workspace with submodules, so which repo/worktree the run used is ambiguous; Source Control must pin to this run's exact working directory
- (idea by coach) Failure: run finished and worktree was cleaned or moved, so files vanish; viewer must say worktree gone not crash
- (direction by user) Set aside failure 1 (secrets); for 2 (repo/submodule) and 3 (worktree gone) read the real Archon code and the running instance, do not guess
- (insight by coach) RUNTIME(mac mini): live API has NO git/file/diff/worktree route, only /api/runs/{runId}/artifacts; the Source Control feature is genuinely new
- (insight by coach) RUNTIME: workflow run row stores working_path (per-run git worktree .../worktrees/archon/thread-<id>), output_root (codebase workspace), and codebase_id; failure-2 answer is pin file reads to run.working_path + codebase_id, do not guess repo root
- (insight by coach) RUNTIME failure risk: same worktree appears under two roots in events, working_path=/Users/agent/.archon/... but 109 tool paths use /Volumes/WD_BLACK/archon/... so path is symlinked/relocatable; UI must realpath and tolerate both, a stale working_path breaks file reads
- (insight by coach) RUNTIME: run status enum is pending|running|completed|failed|cancelled|paused; this run is completed yet worktree path still present, so files may persist after completion (cleanup timing still needs code proof)
- (insight by coach) CODE: a run binds to ONE codebase_id + ONE checkout working_path (nullable); submodules init --recursive into the worktree; folder/--no-worktree projects have null working_path; WorkflowExecution.tsx:344 already reads workingPath (packages/isolation/providers/worktree.ts, workflows/schemas/workflow-run.ts:168-180)
- (insight by coach) Failure-2 answer: pin file reads to run.working_path + codebase_id (authoritative, already stored and exposed at GET /api/workflows/runs/{runId}); must realpath (RUNTIME shows same tree under /Users/agent/.archon and /Volumes/WD_BLACK) and handle null working_path
- (insight by coach) CODE: worktree NOT removed on terminal status; removed by conversation/PR close, scheduler (merged 6h, stale 14d), manual, codebase delete, orphan; destroy deletes checkout + git branch -D; isolation env status is active|destroyed (cleanup-service.ts, isolation/types.ts:28)
- (insight by coach) Failure-3 answer: files can vanish mid-view when a cleanup trigger fires, so check env active|destroyed and worktree exists before read, show 'worktree gone' state; RUNTIME confirms completed run still has files
- (insight by coach) Design consequence: commit HISTORY must be read from the canonical repo (default_cwd) not the worktree, because cleanup runs git branch -D; merged/pushed commits survive there, dangling ones get GC'd
- (insight by coach) Build gap: no git status/diff/log/show or worktree source route exists; @archon/git only has boolean hasUncommittedChanges (git status --porcelain); reuse the artifact pattern GET /api/artifacts/:runId/* + resolveRunArtifactDir as the model for a NEW read-only git API
- (direction by user) Correction: each workflow makes its own worktree, so read history from the worktree, not base default_cwd
- (decision by coach) Corrected earlier claim: read run history/diff/content from run.working_path worktree via git -C working_path (log/show/diff/status); worktree shares object store; default_cwd is on baseBranch dev and does not show the run's branch unless merged
- (insight by coach) Post-cleanup caveat stands separately: after branch -D + checkout removal the run's commits may be unreachable (GC) unless merged/pushed; durable post-cleanup history needs capture-before-teardown, otherwise the run's git view is only available while the worktree lives
- (insight by coach) SOURCE-confirmed (not just OpenAPI absence): raw app.get routes are only /api/stream/__dashboard__, /api/stream/:conversationId, /api/artifacts/:runId/*, webhooks; no git/worktree/diff/source route exists; artifacts route resolves path server-side and rejects .. (api.ts:5071-5078) — the model for a new API
- (insight by coach) Security constraint: new git-read API must resolve the worktree path server-side from runId, never accept working_path from the UI (path-traversal); reuse the artifacts route .. rejection pattern
- (insight by coach) UNVERIFIED: worktree directory existence on the mac mini was not statted (no fs endpoint, no SSH used); DB working_path is only a record, so the UI must handle a missing directory at read time
- (decision by user) Chose path 1 again: run Worst Possible Idea and Crank the Dial to 11
- (technique) started Worst Possible Idea
- (idea by coach) Terrible: ship whole repo to browser on open -> flip: stream server-side, fetch only the requested file/hunks
- (idea by coach) Terrible: make it a full editor that commits from the UI -> flip: strictly read-only inspection, no writes to the run worktree
- (idea by coach) Terrible: build git diff by string-interpolating a user ref into a shell command -> flip: execFileAsync/@archon/git with server-controlled args, never shell-string (project rule)
- (idea by coach) Terrible: when worktree is gone, silently show base repo -> flip: explicit worktree-reaped empty state, never fake the source
- (technique) started Crank the Dial to 11
- (idea by coach) Crank remote-native to 11: reconstruct this run's changed-file view from the workflow EVENT stream (events already carry every tool_input file path + edit), so a change view exists even when git/worktree is gone
- (idea by coach) Crank speed+durability to 11: at run end / per commit, precompute the changed-files+diff and write it as an artifact under output_root (survives cleanup); Source Control reads the durable artifact fast instead of hitting the live worktree, and falls back to live worktree only while it exists
- (decision by coach) Corrected Crank-1: event stream is NOT authoritative for changes (shell/sed/scripts/subprocess/rename/delete mutate files with no structured path event); demote to optional provenance overlay only
- (insight by coach) Truth model: live worktree git is the source of truth for changes/diff/history; a server-generated git snapshot artifact at checkpoints (run end / per commit) under output_root is the durable post-cleanup truth; events are a provenance hint layer (which node/log may relate to a file), never the change list
- (decision by user) Chose path 2: converge, narrow and prioritize into a build-ready spec
- (decision by user) MUST v1: Source Control tab; Changes(M/A)+commit history read from run worktree via git -C working_path; click file opens viewer (M=diff red/green, A=single-pane content); manual Reload; server resolves path from runId reject .. read-only execFileAsync; worktree-gone/null empty state; large+binary files must open
- (decision by user) SHOULD: server-generated git-snapshot artifact at run-end checkpoint under output_root for durable post-cleanup history and fast reads
- (decision by user) COULD: Logs<->Source Control link; Graph node changed-files badge/filter; reuse diff viewer as HITL review; event provenance overlay; default history to this run own commit with full history fallback; collapse empty Changes region on auto-commit runs
- (decision by user) WONT this time: standalone Snapshot mode for M; full Explorer tree of unchanged files; edit/commit from UI; auto-refresh/polling; authoritative change-view reconstructed from events
- (decision by coach) Diff direction fixed: Now = HEAD -> worktree; selected commit = parent -> commit
- (decision by user) Locked the full MoSCoW scope including the two re-added SCAMPER ideas
- (insight by coach) Synthesis: the remote 'no local repo' constraint is the reason the feature exists (not a limitation) and is what separates it from embedding VS Code; the user's '1 run = 1 worktree' fact + persisted working_path is the single pin that both resolves which-repo and enables the whole read model; the manual-Reload + worktree-gone empty state designed early under Constraint Mapping was later validated exactly by the lifecycle cleanup evidence; the Crank durable git-snapshot artifact under output_root is the real breakthrough because it resolves slow + worktree-vanishes + durable-history at once; diff-only M/A viewer is the minimal surface that still answers the JTBD 'did this run touch what I think'
- (decision by coach) The earlier synthesis entry was logged before the user mirror response and is premature/superseded by the final insight below
- (insight by coach) Final synthesis (post-mirror). Connections: (1) the single pin = resolve working_path server-side from runId + realpath ties the user's '1 run = 1 worktree' correctness to the no-UI-path security rule; get this wrong and every view is wrong or unsafe. (2) git-from-worktree + diff-only + M/A viewer + manual Reload is the minimal surface that fully answers the JTBD 'did this run touch what I think'. (3) worktree-gone/null empty state makes it trustworthy on a live remote run. Chosen top-3 v1 priorities = pin, minimal read surface, graceful vanishing. The durable git-snapshot artifact (SHOULD) is the upgrade that turns 'gone' into 'still here' and resolves slow+vanished+durable-history together, but is not v1-critical
