{
  "version": 1,
  "id": "run-deterministic-workflow",
  "description": "Preview a deterministic workflow without creating a run.",
  "impact_paths": ["packages/workflows/src/dry-run*"],
  "behaviors": [
    {
      "id": "workflows.dry-run",
      "description": "Preview a deterministic workflow without creating a run.",
      "priority": "P1",
      "scenarios": ["workflows.dry-run"]
    }
  ],
  "scenarios": [
    {
      "id": "workflows.dry-run",
      "runner": {
        "kind": "cli",
        "recipe": "run-deterministic-workflow"
      },
      "proof_obligations": ["cli-action", "persistence-read-back"]
    }
  ]
}
