interface:
  display_name: "Archon Workflow Creator"
  short_description: "Create valid Archon workflow YAML"
  default_prompt: "Use $archon-workflow-creator to design a bundled default workflow in .archon/workflows/defaults/ and validate it."
