## Codebase Patterns

### Provider usage contract is additive and package-local
- **Where**: `packages/providers/src/types.ts`, `packages/providers/src/usage-breakdown.ts`
- **Pattern**: Add `usageBreakdown?: UsageBreakdown` beside retained `modelUsage`; validate entries in providers only (no workflow/core imports). Missing measures stay `undefined`; known zeros survive; forbidden estimate/context fields are rejected by key name without logging payloads.
- **Example**: `normalizeUsageBreakdown(entries)` returns `{ breakdown, rejected: [{ index, issue }] }`.

### Observability span model never guesses from multi-model usage
- **Where**: `packages/providers/src/observability.ts` `resolveModel`
- **Pattern**: Prefer terminal `resolvedModel.id`, then effective `options.model`, then `assistantConfig.model`, else absent. Do not read `modelUsage` / `usageBreakdown` keys for the span model attribute.
- **Example**: Multi-model result without `resolvedModel` leaves generation model undefined.

### Providers package test isolation
- **Where**: `packages/providers/package.json` `scripts.test`
- **Pattern**: Each new test file gets its own `bun test <file>` invocation chained with `&&` to avoid `mock.module` pollution.
- **Example**: `bun test src/usage-breakdown.test.ts && bun test src/observability.test.ts`

### Claude usage-bearing API failures yield terminal isError with accumulated usage
- **Where**: `packages/providers/src/claude/provider.ts` sendQuery retry loop + `ClaudeApiResultError`
- **Pattern**: Normalize `modelUsage` before API-failure throw; attach `usageBreakdown` on `ClaudeApiResultError`; accumulate across retries; on final typed API failure with usage, yield `isError` result and return (preserve throw when no usage). Merge prior retry usage into successful terminal results.
- **Example**: rate_limit with modelUsage → 4 attempts → one result chunk with 4 breakdown rows

### Grok multi-model usage never apportions aggregates
- **Where**: `packages/providers/src/grok/event-parser.ts` `buildUsageBreakdown`
- **Pattern**: One valid model gets aggregate tokens/USD/requests; multiple models get requests-only rows + one `model: null`/`unknown` aggregate row; no model uses requested model when known else unknown.
- **Example**: two modelCalls keys + usage → two requests rows + one unknown aggregate

### Codex cache subtraction keeps legacy total input
- **Where**: `packages/providers/src/codex/provider.ts` `extractUsageFromCodexEvent`
- **Pattern**: `TokenUsage.input` stays SDK `input_tokens`; breakdown `inputTokens = max(input - cached, 0)` with `cacheReadTokens = cached`; reasoning is subset of output; no requests/USD.
- **Example**: 100/40 cached → legacy input 100, breakdown inputTokens 60


### OpenCode cost must sum distinct assistant messages
- **Where**: `packages/providers/src/community/opencode/session.ts`, `multi-agent.ts`, `tokens.ts`
- **Pattern**: Map assistant info by message id (replace on repeat). At idle, sum `normalizeTokens` for legacy totals and emit one `usageBreakdown` row per id. Top-level `cost` MUST equal `tokens.cost` (sum), because dag-executor accounts only `msg.cost` and discards `tokens.cost`. Reasoning is an output subset — never add it into total again. Multi-agent rows set `kind: 'subagent'`.
- **Example**: two ids cost 0.2 + 0.05 → `tokens.cost` and `cost` both 0.25

### Copilot usage accumulates; multiplier is not USD
- **Where**: `packages/providers/src/community/copilot/event-bridge.ts`
- **Pattern**: Capture every `assistant.usage` into arrays; sum legacy TokenUsage; normalize each entry with `provider: github-copilot`, `requests: 1`, optional cache/reasoning. Ignore `data.cost` (multiplier). `kind: subagent` only for non-empty `parentToolCallId`; never from free-form `initiator`.
- **Example**: two usage events → two breakdown rows; cost field omitted

### Pi catalog cost includes cache + optional tiers
- **Where**: `packages/providers/src/community/pi/model-catalog.ts`, `packages/server/src/routes/schemas/provider.schemas.ts`
- **Pattern**: Expose `cacheRead`/`cacheWrite` always; map SDK `tiers` when present. Route schema requires the cache fields; tiers optional.
- **Example**: `{ input, output, cacheRead, cacheWrite, tiers?: [{ inputTokensAbove, ... }] }`


---

## 2026-09-04 — US-001: Provider usage contract and normalizer

**Status**: PASSED
**Files changed**:
- `packages/providers/src/types.ts` — added `ModelSource`, `ModelUsageEntry`, `UsageBreakdown`, and `usageBreakdown?` on result chunks; retained `modelUsage`
- `packages/providers/src/index.ts` — re-exported new contract types
- `packages/providers/src/usage-breakdown.ts` — provider-local normalizer with absence-preserving validation
- `packages/providers/src/usage-breakdown.test.ts` — contract matrix coverage
- `packages/providers/src/observability.ts` — resolvedModel → options.model → assistant config precedence; no multi-model guess
- `packages/providers/src/observability.test.ts` — precedence and multi-model tests
- `packages/providers/package.json` — isolated `usage-breakdown` test invocation

**Acceptance criteria verified**:
- [x] types.ts defines ModelSource, ModelUsageEntry, UsageBreakdown, and usageBreakdown?: UsageBreakdown while retaining modelUsage
- [x] index.ts re-exports ModelSource, ModelUsageEntry, UsageBreakdown
- [x] usage-breakdown.ts validates provider/model/modelSource consistency, safe integer tokens, positive requests, finite non-negative costUsd, reasoning bounds, at least one numeric measure
- [x] normalizer preserves known zeros and missing fields as undefined
- [x] mixed runtime array retains valid entries in order, rejects by index/issue, does not log raw payloads
- [x] observability resolves span model from resolvedModel → options.model → assistant model → absent; never first multi-model usage key
- [x] tests cover slash ids, unknown models, zero cost, absent fields, unsafe integers, NaN/infinity/negative, zero requests, reasoning bounds, forbidden estimates, modelUsage compatibility, observability precedence
- [x] full `packages/providers` `bun run test` green

**Learnings**:
- Keep `modelUsage` without a `@deprecated` JSDoc tag in this codebase style; a plain compatibility note is enough and avoids unfinished-deprecation lint pressure.
- Issue codes must never embed raw payload values — use `forbidden_field:${key}` only.
- Story gate for shared MessageChunk changes is the full providers package test script, not only the new file.

---

## 2026-09-04 — US-002: Normalize SDK-native providers

**Status**: PASSED
**Files changed**:
- `packages/providers/src/claude/provider.ts` — map SDK modelUsage → usageBreakdown only (no new raw modelUsage producer); retry accumulation; usage-bearing final API error yield
- `packages/providers/src/claude/provider.test.ts` — multi-model/cache/zero/retry/final-error/no-usage-throw coverage
- `packages/providers/src/codex/provider.ts` — turn.completed breakdown with requested model + cache clamp + reasoning
- `packages/providers/src/codex/provider.test.ts` — default result fixtures + cache/reasoning/incomplete coverage
- `packages/providers/src/grok/event-parser.ts` — requested-model context + multi-model safe breakdown
- `packages/providers/src/grok/event-parser.test.ts` — single/multi/none/malformed modelCalls coverage
- `packages/providers/src/grok/provider.ts` — pass effective model into parser

**Acceptance criteria verified**:
- [x] Claude maps every SDK modelUsage entry to anthropic/reported/cache/cost fields; omits requests; keeps failure classification/retry
- [x] Claude carries usage across retries into terminal success and yields accumulated usage on final typed errors; no synthesis without usage
- [x] Codex maps openai + requested/unknown model, cache subtraction, reasoning subset, no requests/USD
- [x] Codex keeps legacy TokenUsage.input as total input; no usage on incomplete/failed turns
- [x] Grok uses requested model context; single/multi/no-model cases; never apportions aggregates
- [x] Grok preserves raw modelUsage alongside usageBreakdown
- [x] Provider tests cover the AC matrix; full packages/providers bun run test green
- [x] lint --max-warnings 0 and format:check green

**Learnings**:
- Existing Codex `toEqual` result fixtures must include `usageBreakdown` once turn.completed always emits it.
- Usage-bearing Claude API finals change from throw→isError yield only when accumulated usage exists; no-usage auth/rate-limit tests keep throw semantics.
- Grok multi-model rows without positive `modelCalls` are skipped for requests-only emission; single-model still attaches aggregates even without requests.
- Plan forbids new raw `modelUsage` producers; only Grok keeps its existing raw emission. Claude/Codex emit `usageBreakdown` only.

---

## 2026-09-04 — US-003: Normalize community SDK providers and Pi catalog

**Status**: PASSED
**Files changed**:
- `packages/providers/src/community/pi/event-bridge.ts` — multi-message usageBreakdown + summed legacy tokens
- `packages/providers/src/community/pi/event-bridge.test.ts` — multi-message/responseModel/error coverage
- `packages/providers/src/community/pi/model-catalog.ts` — cache rates + optional tiers
- `packages/providers/src/community/pi/index.ts` — export cost types
- `packages/providers/src/community/opencode/tokens.ts` — reasoning subset fix + entry helpers
- `packages/providers/src/community/opencode/tokens.test.ts` — unit coverage
- `packages/providers/src/community/opencode/session.ts` — per-message-id map + summed cost
- `packages/providers/src/community/opencode/multi-agent.ts` — subagent observations + summed cost
- `packages/providers/src/community/opencode/provider.test.ts` — multi-id and total expectations
- `packages/providers/src/community/copilot/event-bridge.ts` — accumulate usage events
- `packages/providers/src/community/copilot/event-bridge.test.ts` — multi-event/parent-tool/multiplier tests
- `packages/server/src/routes/schemas/provider.schemas.ts` — additive Pi cost schema
- `packages/server/src/routes/api.providers.test.ts` — catalog schema tests
- `packages/web/.../skills/providers.ts` + fixture tests — PiModelInfo cache fields
- `packages/providers/package.json` — isolated tokens.test invocation

**Acceptance criteria verified**:
- [x] Pi one observation per assistant message; responseModel>model>unknown; requests 1; full usage fields
- [x] Pi sums legacy tokens/cost; last-assistant stop/error/resolvedModel
- [x] OpenCode per-id map; one observation each; multi-agent kind subagent; no reasoning double-count
- [x] OpenCode top-level cost equals summed tokens.cost for dag-executor accounting
- [x] Copilot accumulates usage events; github-copilot; optional fields; ignore multiplier; parentToolCallId only for subagent
- [x] Qoder unchanged
- [x] Pi catalog cache-read/write + tiers; route schema/tests updated
- [x] Tests cover multi Pi messages, OpenCode repeated/multi-id, Copilot multi-event/parent/multiplier/optional, no reasoning double charge
- [x] full packages/providers bun run test green; lint/typecheck/format clean on changed files

**Learnings**:
- dag-executor uses `msg.cost`, not `tokens.cost` — OpenCode top-level cost must be the multi-message sum.
- OpenCode fixture `cache: 1` was a legacy shape; real SDK is `{ read, write }`.
- api.generated.d.ts is auto-generated and blocked from direct edit; schema + inline console type updated; regen deferred until server generate:types.

---
