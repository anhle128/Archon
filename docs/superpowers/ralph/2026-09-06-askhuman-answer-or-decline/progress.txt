## Codebase Patterns

### Terminal purge is query-scoped; workflows.ts calls in
- **Where**: `packages/core/src/db/workflow-pending-interactions.ts`, `packages/core/src/db/workflows.ts`
- **Pattern**: `purgePendingInteractionsInTransaction` takes the caller's `query` and must not open a nested transaction. It selects pending rows in store order (`created_at ASC, id ASC`), CAS-updates only `pending` → `purged` with `resolved_at`, leaves `answer`/`resolved_by` null, and writes one id-only `interaction_resolved` event. `workflows.ts` may import this helper; the reverse import is a cycle. Call purge only after a winning status CAS. `failOrphanedRuns` locks running ids first, updates that set, then purges each selected id.
- **Example**: `await purgePendingInteractionsInTransaction(query, id, 'cancelled')`

### Query-scoped resume is shared, callers own the transaction
- **Where**: `packages/core/src/db/workflow-resume-transition.ts`
- **Pattern**: `resumeWorkflowRunInTransaction` takes an already-open `query`. `'standard'` keeps failed/paused/cancelled/stale-running; `'paused-ask'` matches only `status = 'paused'`. `workflow-pending-interactions.ts` must not import `workflows.ts` (cycle). Public `resumeWorkflowRun` still owns CAS-miss probe and final select.
- **Example**: `resumeWorkflowRunInTransaction(query, id, dialect, 'paused-ask')`

### Canonical AskHuman contracts live in the engine schema module
- **Where**: `packages/workflows/src/schemas/pending-interaction.ts`
- **Pattern**: Import `z` from `@hono/zod-openapi` and derive types with `z.infer`. Answer/resolve objects use `.strict()`. `askHumanQuestionSchema` is exported without `.strict()` so AskHuman's accepted question shape stays unchanged. `@archon/providers/types` may add `ResumeInteraction` but must not import `@archon/workflows`.
- **Example**: `askAnswerBodySchema` is a two-arm union of `{ answers: [...] }` (min 1) and `{ decline: true }`; mixed bodies fail because both arms are strict.

### Providers contract types stay SDK-free
- **Where**: `packages/providers/src/types.ts` (`SendQueryOptions`)
- **Pattern**: Add provider-neutral resume fields on `SendQueryOptions`; do not thread answers through node prompts.
- **Example**: `resumeInteractions?: readonly ResumeInteraction[]`

---

## 2026-09-06 — US-001: Define AskHuman answer and provider contracts

**Status**: PASSED
**Files changed**:
- `packages/workflows/src/schemas/pending-interaction.ts` — added ask answer, question, and resolve schemas with `z.infer` types
- `packages/workflows/src/schemas/pending-interaction.test.ts` — variant, multi, rejection, unknown-key, and type-inference coverage
- `packages/workflows/src/ask-human.ts` — re-imported `askHumanQuestionSchema` without changing accepted shape
- `packages/providers/src/types.ts` — added `ResumeInteraction` and `SendQueryOptions.resumeInteractions`

**Acceptance criteria verified**:
- [x] pending-interaction.ts defines askAnswerItemSchema, askAnswerBodySchema, resolvePendingInteractionInputSchema, and resolvePendingInteractionResultSchema using z from `@hono/zod-openapi` and z.infer
- [x] askAnswerBodySchema accepts answers (min 1) or `{ decline: true }`, and rejects empty answers, false decline, and mixed unions
- [x] askHumanQuestionSchema is exported from pending-interaction.ts and re-imported into ask-human.ts without altering its accepted shape
- [x] ResumeInteraction and optional resumeInteractions added to SendQueryOptions
- [x] pending-interaction.test.ts verifies acceptance, type inferences, and unknown-key/invalid-shape rejection
- [x] `bun test src/schemas/pending-interaction.test.ts` and `bun test src/ask-human.test.ts` in packages/workflows, and `bun x tsc --noEmit` in packages/providers

**Learnings**:
- Fresh worktree had no node_modules; `bun install` from the lockfile is required before focused tests.
- ESLint ignores test files when they are passed as explicit paths; lint production sources only (or use `--no-warn-ignored`).
- `askHumanQuestionSchema` must not gain `.strict()`; that would change the existing AskHuman accepted shape.

---

## 2026-09-06 — US-002: Implement atomic answer CAS and shared resume primitive

**Status**: PASSED
**Files changed**:
- `packages/core/src/db/workflow-resume-transition.ts` — shared resume primitive, eligibility, lock/status clauses
- `packages/core/src/db/workflows.ts` — delegates resume SQL to the shared primitive; keeps CAS-miss probe and final select
- `packages/core/src/db/workflow-pending-interactions.ts` — `resolvePendingInteraction` with envelope validation, row CAS, last-pending paused-ask resume, id-only `interaction_resolved`
- `packages/core/src/db/workflow-pending-interactions.test.ts` — winning path, rollback, sibling pending, validation, redaction

**Acceptance criteria verified**:
- [x] workflow-resume-transition.ts exports WorkflowTransactionQuery, ResumeEligibility, ORPHAN_RESUME_STALE_DAYS, resumableWorkflowStatusClause, workflowRunLockClause, resumeWorkflowRunInTransaction
- [x] workflows.ts delegates resume SQL and status predicates while preserving public resumeWorkflowRun behavior
- [x] resolvePendingInteraction uses withTransaction and locks run + interaction rows (FOR UPDATE on Postgres)
- [x] typed errors: NotFound, AlreadyResolved, RunNotPaused, ValidationError
- [x] semantic envelope validation without logging prompt/answer sentinels
- [x] CAS to answered, last-pending paused-ask resume only when remaining pending is zero
- [x] interaction_resolved event with step_name = node_id and non-sensitive data
- [x] bun test workflow-pending-interactions.test.ts, workflows.test.ts, workflows.resume-cas.integration.test.ts

**Learnings**:
- SQLite `RAISE(IGNORE)` on BEFORE UPDATE is a reliable way to force a resume CAS miss inside the same transaction without mocking `executeQuery`.
- Do not destructure snake_case schema fields into locals; eslint naming-convention requires camelCase aliases.
- `isDecline` must be a type predicate or `answer.answers` does not narrow after the early return.

---

## 2026-09-06 — US-003: Purge pending interactions on terminal transitions

**Status**: PASSED
**Files changed**:
- `packages/core/src/db/workflow-pending-interactions.ts` — added `purgePendingInteractionsInTransaction`
- `packages/core/src/db/workflow-pending-interactions.test.ts` — pending-only purge + safe event assertion
- `packages/core/src/db/workflows.ts` — cancel/fail/orphan/reject-cancel call purge after winning CAS
- `packages/core/src/db/workflows.test.ts` — winning purge vs no-op, complete does not purge
- `packages/core/src/db/workflows.resume-cas.integration.test.ts` — real SQLite lifecycle purge coverage

**Acceptance criteria verified**:
- [x] purgePendingInteractionsInTransaction transitions only pending rows to purged, sets resolved_at, writes one interaction_resolved event per row
- [x] Purge events contain { node_id, tool_use_id, kind: 'ask', purged: true, resumed: false, terminal_status } and omit envelope/answer data
- [x] cancelWorkflowRun, cancelRecoveryWorkflowRun, resolveAndCancelApprovalGate, failWorkflowRun, and failOrphanedRuns invoke purge inside the winning transaction
- [x] No-op or non-winning status transitions do not purge rows or emit purge events
- [x] completeWorkflowRun does not purge interactions
- [x] bun test workflow-pending-interactions.test.ts, workflows.test.ts, workflows.resume-cas.integration.test.ts

**Learnings**:
- Keep envelope out of the purge SELECT so a corrupt stored question cannot block cancel/fail.
- failOrphanedRuns must lock and collect running ids first, then UPDATE that locked set, then purge each selected id — a single unscoped UPDATE cannot attach per-run purge events correctly.

---
