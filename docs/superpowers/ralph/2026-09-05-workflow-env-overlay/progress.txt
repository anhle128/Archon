## Codebase Patterns

### Symbol-preserving node clone
- **Where**: `packages/workflows/src/include-expander.ts` (`cloneNodeWithEngineMetadata`)
- **Pattern**: `structuredClone` drops symbol keys. Re-attach `COMPOSED_NODE` and nested `COMPILED_LOOP_COMMAND` (including loop_group body recursion) after clone. Shared by include expansion and ENV overlay — do not duplicate the private symbol list.
- **Example**: `cloneNodeWithEngineMetadata(node)` / `cloneWorkflowWithEngineMetadata(workflow)`

### ENV overlay field matrix
- **Where**: `packages/workflows/src/env-overlay.ts`
- **Pattern**: Allowed fields follow executor behavior, not the broad base Zod object: prompt→prompt/provider/model/effort/thinking; command→provider/model/effort/thinking; loop→provider/model/effort; loop_group→provider/model; bash→bash; other→none. Missing top-level ids skip; incompatible existing ids fail closed with safe codes (no prompt/bash bodies in errors).
- **Example**: `applyEnvOverlay(workflow, patches)` → `{ workflow, appliedPatches, missingNodeIds }`

### Empty prompt/bash vs dagNodeSchema
- **Where**: `packages/workflows/src/schemas/dag-node.ts` mode detection + `env-overlay.ts` `isEmptyBodyOnlyFailure`
- **Pattern**: `dagNodeSchema` treats whitespace-only prompt/bash as modeCount 0 and rejects. ENV contracts still allow empty/whitespace bodies byte-for-byte — after assign, accept only those known empty-body issues; other schema failures stay `invalid_patched_node`.
- **Example**: empty bash apply succeeds; malformed effort still fails

### Zod overlay schema conventions
- **Where**: `packages/workflows/src/schemas/env-overlay.ts`
- **Pattern**: Import `z` from `@hono/zod-openapi`; `z.infer`; explicit `z.record(keySchema, valueSchema)`; `.strict()` documents; stored overlay union tries complete snapshot before pending applied form.
- **Example**: `storedEnvOverlaySchema = z.union([envOverlaySnapshotSchema, appliedEnvOverlaySchema])`

### Shared NodeExecutionMetadata resolution
- **Where**: `packages/workflows/src/node-model-resolution.ts`
- **Pattern**: `resolveNodeExecutionRequest` is the pure chain + capability-aware effort/thinking used by `resolveNodeProviderAndModel`, `node_started`, ENV preview, and ENV snapshot. `buildResolvedRequestMetadata` walks prompt/command/loop + nested group bodies with qualified ids (`group.child`); excludes group containers and deterministic nodes. `resolveGroupModelScope` forwards group provider/model/preset/tier while preserving outer effort.
- **Example**: `request.metadata` === `node_started.data` request fields; body scope = `resolveGroupModelScope(group, outerScope, …)`

### Workflow ENV store boundaries
- **Where**: `packages/core/src/db/workflow-envs.ts`
- **Pattern**: Normalize PG JSONB objects and SQLite JSON strings through `envPatchesSchema` before row parse. Corrupt rows throw `WorkflowEnvCorruptRowError` with id-only logs (never patch bodies). Name conflicts map only when PG `constraint === 'uq_workflow_envs_workflow_name_name'` or SQLite UNIQUE message names both identity columns. Update/delete always dual-predicate `(id, workflow_name)`; patches whole-document replace; empty `{}` allowed; empty update body rejected. List omits patches and orders `LOWER(name), name, id`.
- **Example**: `updateWorkflowEnv(wf, id, { patches: {} })` clears the map; mismatched workflow returns null without mutating.

### Atomic run envOverlay replacement
- **Where**: `packages/workflows/src/store.ts` (`IWorkflowEnvOverlayStore`), `packages/core/src/db/workflows.ts` (`setWorkflowRunEnvOverlay`)
- **Pattern**: Do not use `updateWorkflowRun` / `json_patch` for nested overlay writes — SQLite deep-merges and stale `resolved` keys survive. Replace only `metadata.envOverlay` via PG `jsonb_set(..., '{envOverlay}', $1::jsonb, true)` or SQLite `json_set(..., '$.envOverlay', json($1))`; UPDATE then SELECT in one transaction; no SQLite RETURNING; rowCount !== 1 throws.
- **Example**: `store.setWorkflowRunEnvOverlay(runId, completeSnapshot)` → full run row with siblings preserved

### Executor overlay ownership
- **Where**: `packages/workflows/src/executor.ts` (`prepareExecutionEnvOverlay`, complete snapshot before `workflow_started`)
- **Pattern**: Caller-supplied `appliedEnvOverlay` → verify only (workflow already patched). Else parse `preCreatedRun.metadata.envOverlay` and reapply frozen patches. Overlay-bearing preCreated runs use `preCreatedRun.user_id` for prefs/creds; no-overlay keeps `opts.userId`. Pending applied form is stamped at first INSERT; complete snapshot with live `resolved` is written via `setWorkflowRunEnvOverlay` before any start event/DAG. Audit/prepare failure: fail-closed, no execution. Fresh children never copy parent overlay.
- **Example**: `executeWorkflow(..., { appliedEnvOverlay })` or resume with `preCreatedRun.metadata.envOverlay` only

### Orchestrator pre-isolation ENV gate
- **Where**: `packages/core/src/orchestrator/env-overlay-dispatch.ts`, `orchestrator-agent.ts` (`dispatchOrchestratorWorkflow`)
- **Pattern**: Frozen `EnvOverlayCandidate` rides `HandleMessageContext`/`WorkflowDispatchOptions` out-of-band (never the command string). After input/requirement gates and before conversation mutation/`validateAndResolveIsolation`: fresh → `resolveFreshDispatchEnvOverlay`; continuation → `resolveContinuationDispatchEnvOverlay` on run `metadata.envOverlay` (ignore request candidate + notify). Fail closed with safe codes. Hand off patched clone + detached `AppliedEnvOverlay` to fg `executeWorkflow` and bg `dispatchBackgroundWorkflow` (pre-create stamp already US-005). Hydrate-null fallthrough uses pre-validated request candidate, not the prior run overlay.
- **Example**: `options.envOverlay` → apply before isolation → `executeWorkflow(..., executionWorkflow, { appliedEnvOverlay })`

### HTTP Workflow ENV CRUD + preview
- **Where**: `packages/server/src/routes/api.ts`, `packages/server/src/routes/schemas/workflow-env.schemas.ts`, `packages/workflows/src/env-overlay.ts` (`listEnvOverlayTargets`)
- **Pattern**: OpenAPI CRUD under `/api/workflows/{name}/envs` with camelCase wire mapper; list omits patches; create provenance via soft `resolveWebUserId` (null OK). Preview is a separate `/env-preview` route requiring `validateCwd`, discover+`resolveWorkflowName`, optional ENV identity (`400 env_not_found` / `env_workflow_mismatch`), `listEnvOverlayTargets` for editor matrix (top-level only), `applyEnvOverlay` + executor-parity `buildResolvedRequestMetadata`/`buildAiProfile` (corrupt prefs → config-only). Never echo prompt/bash in errors/responses.
- **Example**: `GET .../env-preview?cwd=...&envId=...` → `{ preview:true, authoritative:false, targets, resolved, skippedNodeIds }`

### Start route ENV freeze
- **Where**: `packages/server/src/routes/api.ts` (`POST /api/workflows/{name}/run`)
- **Pattern**: Parse optional `envId` (JSON string or multipart plain field; empty/omitted = YAML-only; array/non-string = 400 request-shape). Load unscoped row after field parse and BEFORE file/message persistence. Missing → `400 env_not_found`; `row.workflow_name !==` decoded route name → `400 env_workflow_mismatch`; corrupt → `500 env_store_corrupt`. Re-parse `structuredClone(row.patches)` via `envPatchesSchema` into a new tree, `Object.freeze` candidate `{ envId, envName, workflowName, patches }`, pass as `HandleMessageContext.envOverlay`. Do not discover/apply YAML on Start.
- **Example**: `extraContext.envOverlay` → orchestrator US-006 gate








---

## 2026-09-05 — US-001: Engine overlay schemas and apply module

**Status**: PASSED
**Files changed**:
- `packages/workflows/src/schemas/env-overlay.ts` — strict patch/candidate/applied/snapshot/resolution schemas + bounds
- `packages/workflows/src/schemas/env-overlay.test.ts` — schema bounds, reserved keys, lifecycle union
- `packages/workflows/src/env-overlay.ts` — pure clone/apply, field matrix, safe EnvOverlayError codes
- `packages/workflows/src/env-overlay.test.ts` — matrix, include ids, group-body skip, isolation, graph validation
- `packages/workflows/src/include-expander.ts` — export generalized `cloneNodeWithEngineMetadata`
- `packages/workflows/src/compiled-command.ts` — comment names shared clone helper
- `packages/workflows/src/schemas/index.ts` — re-export overlay schemas/types
- `packages/workflows/package.json` — `./env-overlay` export + test segment

**Acceptance criteria verified**:
- [x] env-overlay schemas export strict Zod types via `@hono/zod-openapi` + `z.infer` + explicit record keys
- [x] Patch validation allowlist + rejects reserved/empty keys, empty per-node patch, >256 targets, >1MiB, unknown keys; accepts long include ids and empty/whitespace prompt/bash
- [x] Symbol-preserving clone of roots/nodes/nested groups via generalized helper
- [x] Apply matches top-level ids only, skips missing sorted, rejects incompatible fields/unknown providers, validates nodes+graph, safe error codes without bodies
- [x] Tests cover matrix, include ids, group-body skip, graph validation, deep-copy, ENV A/B/no-ENV isolation

**Learnings**:
- `dagNodeSchema` rejects empty/whitespace prompt/bash via mode detection even though field schemas are plain `z.string()` — ENV apply must special-case those known issues only.
- Thinking shorthand should be normalized through `thinkingConfigSchema` in the applied map so storage/API and runtime agree.
- Include-expanded target keys must not reuse the 64-char `safeNodeIdSchema` cap.

---

## 2026-09-05 — US-002: Shared request metadata and loop-group parity

**Status**: PASSED
**Files changed**:
- `packages/workflows/src/node-model-resolution.ts` — pure `resolveNodeExecutionRequest`, `resolveGroupModelScope`, `buildResolvedRequestMetadata`
- `packages/workflows/src/node-model-resolution.test.ts` — precedence, effort fail-closed, thinking warning semantics, nested resolved map
- `packages/workflows/src/dag-executor.ts` — consume shared metadata for node_started; forward full group scope to body
- `packages/workflows/src/dag-executor.test.ts` — loop_group model/tier body forwarding regression
- `packages/workflows/package.json` — test segment includes resolution tests

**Acceptance criteria verified**:
- [x] One pure resolution result used by resolveNodeProviderAndModel, node_started, ENV preview/snapshot builders
- [x] NodeExecutionMetadata preserves provider/model/tier/modelReasoningEffort/effort/thinking; unsupported effort fails; unsupported thinking kept with warning flag
- [x] Resolved map covers top-level prompt/command/loop + nested group bodies with qualified ids; excludes containers/bash
- [x] loop_group provider/model/preset/tier forwarded recursively; outer effort/thinking preserved; group effort/thinking unsupported
- [x] Tests prove parity across precedence, rejected effort, assistant fallback, thinking, loops, nested groups

**Learnings**:
- Loop node_started previously stuffed assistant fallback into `effort`; unified path uses `modelReasoningEffort` like prompt/command.
- Group dispatch must rebuild WorkflowModelScope (provider/model/preset/tier), not only provider — previously model/preset/tier stayed outer.
- `registerOpencodeProvider` needed in pure unit tests for effortControl:false (not in builtins).

---

## 2026-09-05 — US-003: Workflow ENV database schema and CRUD store

**Status**: PASSED
**Files changed**:
- `migrations/000_combined.sql` — table 21 after usage_ledger; index + COMMENT ON COLUMN in trailing section; inventory 21/22–25
- `packages/core/src/db/adapters/sqlite.ts` — createSchema twin table/index
- `packages/core/src/db/adapters/sqlite.test.ts` — MIN_NON_AUTH_COLUMNS 155→162
- `packages/core/src/db/bundled-schema.generated.ts` — regenerated
- `packages/core/src/schemas/workflow-env.ts` + test — name/workflow-name/row/summary schemas
- `packages/core/src/db/workflow-envs.ts` + test — CRUD, JSON normalize, corrupt/conflict, scoping
- `packages/core/src/schemas/index.ts`, `packages/core/src/db/index.ts`, `packages/core/package.json` — exports + test segments
- `AGENTS.md` — 21 application tables inventory

**Acceptance criteria verified**:
- [x] Postgres remote_agent_workflow_envs after usage_ledger; unique (workflow_name, name); index/comments in final section
- [x] SQLite createSchema mirror; inventory 21; parity floor 162; bundled schema regenerated
- [x] Core schemas: workflow name ≤255 + isValidWorkflowName; ENV name trim 1–64 regex; envPatchesSchema; empty {} valid
- [x] list summaries without patches ORDER BY LOWER(name), name, id; get-by-id; create; scoped update whole-doc replace; scoped delete
- [x] JSONB/TEXT normalize; WorkflowEnvCorruptRowError id-only log; constraint/message-exact name conflict
- [x] Tests: fresh SQLite schema, parity suite, CRUD round-trip, bounds, corrupt/summary independence, full replace, mismatch scoping

**Learnings**:
- SqliteAdapter `PRAGMA table_info(...)` returns empty via parameterized query path — use `pragma_table_info('table')` or raw bun:sqlite for introspection tests.
- Portable UPDATE CASE flags must avoid PG-only `::int` casts so SQLite accepts the same SQL.
- New table indexes belong in createSchema (SQLite) / trailing Indexes section (Postgres), never migrateColumns for brand-new tables.

---

## 2026-09-05 — US-004: Atomic run envOverlay metadata replacement

**Status**: PASSED
**Files changed**:
- `packages/workflows/src/store.ts` — `IWorkflowEnvOverlayStore` + `IWorkflowStore` extends it
- `packages/core/src/db/workflows.ts` — `setWorkflowRunEnvOverlay` (PG jsonb_set / SQLite json_set+json, txn UPDATE+SELECT)
- `packages/core/src/db/workflows.test.ts` — PG SQL shape, not-found, invalid snapshot unit tests
- `packages/core/src/db/workflows.env-overlay.integration.test.ts` — real SQLite nested replace + sibling preserve + not-found; paths mock spreads real exports
- `packages/core/src/workflows/store-adapter.ts` + test — delegates method; unnested delegation test
- `packages/workflows` mock stores (executor/subrun/dag-executor/script-node-deps/preamble) — implement method
- `packages/core/package.json` — isolated `bun test` segment for env-overlay integration

**Acceptance criteria verified**:
- [x] IWorkflowStore extends required IWorkflowEnvOverlayStore with setWorkflowRunEnvOverlay
- [x] Core adapter + every concrete in-memory test store/mock implements the method
- [x] PG jsonb_set / SQLite json_set+json replace only metadata.envOverlay
- [x] Both dialects: one transaction UPDATE then SELECT, no SQLite RETURNING, rowCount===1 required
- [x] Integration: second complete snapshot drops stale resolved keys, preserves siblings, run-not-found throws

**Learnings**:
- Incomplete `mock.module('@archon/paths')` factories break SqliteAdapter init when new path exports land — always spread `...realArchonPaths` like resume-cas.
- Nested `test()` inside another test is a silent structure bug Bun rejects at runtime.
- Never use top-level metadata merge for envOverlay; SQLite json_patch deep-merge keeps stale nested keys.

---

## 2026-09-05 — US-005: Executor snapshot ownership and universal replay

**Status**: PASSED
**Files changed**:
- `packages/workflows/src/env-overlay.ts` — parse/verify/restore/snapshot helpers for run-owned overlays
- `packages/workflows/src/env-overlay.test.ts` — restore missing ids, verify drift, snapshot detach
- `packages/workflows/src/executor.ts` — appliedEnvOverlay option, restore/verify entry, pending INSERT, complete snapshot, overlay identity
- `packages/workflows/src/executor.test.ts` — fresh/resume/missing/identity/audit-fail/no-overlay child cases
- `packages/workflows/src/subrun.test.ts` — child create does not inherit parent envOverlay
- `packages/core/src/orchestrator/orchestrator.ts` — background pre-create stamps pending overlay + forwards option

**Acceptance criteria verified**:
- [x] ExecuteWorkflowOptions accepts appliedEnvOverlay; fresh foreground/fallback and background pre-create stamp pending metadata at first INSERT
- [x] Executor restores metadata.envOverlay when caller did not apply; verifies identity/fields when appliedEnvOverlay supplied
- [x] Overlay-bearing preCreated runs use run.user_id for prefs/creds; no-overlay keeps opts.userId
- [x] Complete resolved snapshot atomically written before workflow_started/DAG; audit failure fails closed with no execution
- [x] Resume records latestMissingNodeIds without mutating frozen patches; skipped ids stay skipped
- [x] Tests: resume restore, audit-write failure, child no-inherit, identity, missing ids, pure helpers

**Learnings**:
- Reassigning `let workflowRun` after CFA-narrowing makes later `.catch` closures see `WorkflowRun | undefined` — capture `ownedRunId` before reassignment.
- subrun.test.ts and executor.test.ts must never share a `bun test` process (`mock.module` on dag-executor).
- Background pre-create must stamp pending overlay itself because executor never re-enters the fresh-create branch when `preCreatedRun` is set.

---

## 2026-09-05 — US-006: Orchestrator pre-isolation overlay gate and background handoff

**Status**: PASSED
**Files changed**:
- `packages/core/src/types/index.ts` — `HandleMessageContext.envOverlay?: EnvOverlayCandidate`
- `packages/core/src/orchestrator/env-overlay-dispatch.ts` — pure fresh/continuation overlay resolve + safe message formatter
- `packages/core/src/orchestrator/env-overlay-dispatch.test.ts` — unit coverage for apply, skip, mismatch, resume ignore, corrupt snapshot
- `packages/core/src/orchestrator/orchestrator-agent.ts` — pre-isolation gate; thread patched workflow + AppliedEnvOverlay to fg/bg; resume ignore notice
- `packages/core/src/orchestrator/orchestrator-agent.test.ts` — isolation spy: invalid/mismatch stop before isolation; valid/missing-id/resume/background handoff
- `packages/core/package.json` — isolated test segment for env-overlay-dispatch

**Acceptance criteria verified**:
- [x] HandleMessageContext and WorkflowDispatchOptions carry frozen EnvOverlayCandidate out-of-band
- [x] dispatchOrchestratorWorkflow applies after input/requirement gates and before conversation mutation/isolation
- [x] Continuations ignore newly supplied ENV with notice; fresh uses request; canonical mismatch fails safely
- [x] Unsupported fields/unknown providers/invalid nodes/graph stop before isolation with safe codes (id/name/applied/skipped/error codes only in logs)
- [x] Fg/bg receive patched clone + detached AppliedEnvOverlay; bg pre-create stamp already equivalent (US-005)
- [x] Tests spy on isolation — not called for invalid overlays; valid and missing-id retain isolation

**Learnings**:
- Name `validateAndResolveIsolation` mock so call-count assertions work; anonymous `mock()` inside `mock.module` cannot be cleared/asserted.
- `unknown | undefined` trips `@typescript-eslint/no-redundant-type-constituents` — use `unknown` alone.
- Hydrate-null fallthrough must use the request candidate (fresh row), not the prior run's stored overlay — pre-validate the candidate even when `willContinueExistingRun` so isolation is not wasted on a bad request that only surfaces after hydrate.

---

## 2026-09-05 — US-007: HTTP CRUD and preview API

**Status**: PASSED
**Files changed**:
- `packages/server/src/routes/schemas/workflow-env.schemas.ts` — OpenAPI CRUD/preview request+response schemas
- `packages/server/src/routes/api.ts` — ENV list/get/create/update/delete + env-preview handlers, camelCase mappers, stable error codes
- `packages/server/src/routes/api.workflow-envs.test.ts` — auth gate, CRUD scoping/conflicts, preview cwd/identity/matrix, OpenAPI paths
- `packages/server/package.json` — isolated test segment for ENV routes
- `packages/workflows/src/env-overlay.ts` — export `listEnvOverlayTargets` field matrix helper
- `packages/workflows/src/env-overlay.test.ts` — top-level targets exclude loop_group bodies
- `packages/workflows/package.json` — export `./node-model-resolution` for server preview imports

**Acceptance criteria verified**:
- [x] registerOpenApiRoute + workflow-env.schemas.ts; no per-resource ACL (auth gate only)
- [x] CRUD GET list/detail, POST 201, PATCH, DELETE with 200/201/404/409 and `{ error, detail? }`
- [x] List omits patches; detail/create/update camelCase WorkflowEnvResponse; delete `{ deleted }`; create provenance server-resolved
- [x] CRUD validates name/path/patch/size; no cwd or discoverability required
- [x] Preview requires validated cwd, canonical resolve, baseline or ENV apply, targets/skips/resolved via shared metadata path
- [x] Tests: auth gate, conflicts/scoping, preview root/descendant cwd, identity errors, no prompt/bash echo, OpenAPI CRUD/preview with Start path retained

**Learnings**:
- Preview resolution needs registered providers in tests (`registerBuiltinProviders`) or `buildResolvedRequestMetadata` fails unknown_provider with empty registry.
- Soft header identity uses `findOrCreateUserByPlatformIdentity` — mock that symbol for create-provenance tests, not a bare findOrCreateUser.
- Export `listEnvOverlayTargets` from env-overlay rather than duplicating the private field matrix in server.

---

## 2026-09-05 — US-008: Start route envId freeze and orchestrator handoff

**Status**: PASSED
**Files changed**:
- `packages/server/src/routes/schemas/workflow.schemas.ts` — optional `envId` on unwired `runWorkflowBodySchema`
- `packages/server/src/routes/api.ts` — parse envId JSON/multipart; freeze ENV before file/message side effects; hand off `envOverlay` context; route description
- `packages/server/src/routes/api.workflow-runs.test.ts` — workflow-envs mock + no-ENV/JSON/multipart/malformed/duplicate/missing/mismatch/corrupt/frozen-copy coverage

**Acceptance criteria verified**:
- [x] Optional envId in JSON and multipart; omitted/empty = YAML-only; non-string/duplicated multipart → 400 request-shape
- [x] When envId present, load/validate after parse and before file or run-start message persistence
- [x] Missing → 400 env_not_found; mismatch → 400 env_workflow_mismatch; corrupt fails before side effects
- [x] Frozen candidate has id/name/workflowName + newly allocated patches; store mutation after Start does not change context
- [x] Route does not discover/apply YAML; compatibility errors stay on dispatch/SSE
- [x] Tests cover no-ENV, JSON ENV, multipart ENV, malformed/duplicate, missing, mismatch, corrupt-before-side-effects, frozen-copy

**Learnings**:
- Multipart `parseBody({ all: true })` turns duplicate `envId` into an array — refuse arrays as request-shape, same as non-string JSON.
- Start identity-checks the decoded route parameter only (no discovery/canonical rename); preview still checks both route + canonical.
- Defer `persistUploadedFiles` until after ENV identity so missing/mismatch never leaves upload debris.

---

