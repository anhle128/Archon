| Original Text                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Revised Text                                                                                                                                                                                                                                                                                                                                                                                                                                             | Changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EXPERIENCE.md:247 — "- **`Stopping…` may dim but may not vanish.** SC 1.4.3 exempts a disabled control from the contrast minimum by its own wording, so the dim state is allowed; removing the control instead would move every sibling under the pointer and lose the reader's place."                                                                                                                                                                                             | "- **`Stopping…` may dim but may not vanish.** It stays focusable as `aria-disabled`, so SC 1.4.3's inactive-component exemption does not apply and the dim floor is `--text-secondary`, never tertiary; removing the control instead would move every sibling under the pointer and lose the reader's place."                                                                                                                                           | The rule is right, the justification is stale. Two bullets above, :245 fixes `Stopping…` to `aria-disabled`, **never** the `disabled` attribute — so the disabled-control exemption this sentence leans on no longer applies. Aligns the copy to the verified decision: DESIGN.md:629 ("the SC 1.4.3 inactive-component exemption is no longer being leaned on, which is why the dim floor is `--text-secondary` (**5.33:1 / 7.90:1**) rather than tertiary (**2.31:1 / 3.88:1**)") and the `stop-control` comment at DESIGN.md:349-351. As written, a builder who reads this bullet and not :245 may dim to tertiary at 2.31:1 — the exact regression that fix removed. |
| EXPERIENCE.md:367 — "The control reads `Stopping…` and disables." Also :372 — "the control is disabled and says `answer the agent's question first`" — and :400 — "`Stopping…` disabled, no progress bar, one polite announcement".                                                                                                                                                                                                                                                 | :367 → "The control reads `Stopping…` and goes `aria-disabled`." :372 → "the control is `aria-disabled` and says `answer the agent's question first`". :400 → "`Stopping…` held focusable with `aria-disabled`, no progress bar, one polite announcement".                                                                                                                                                                                               | Three locations, one failure mode, same as the row above. The flow narrative and the resolved-questions table still say the control "disables", which reads as the native attribute and contradicts :130, :175, :245 and DESIGN.md:629. The flow and the Q&A table are the two surfaces a builder skims before reading Component Patterns, so they are where the losing side does the most damage.                                                                                                                                                                                                                                                                       |
| mockups/key-steering-dock.html:199 — `the <span class="mono">you</span> label says who wrote it, and full-strength text against the model's secondary says it again` (identical text in `.working/key-steering-dock.html:199`)                                                                                                                                                                                                                                                      | `the <span class="mono">operator</span> label says who wrote it, and full-strength text against the model's secondary says it again`                                                                                                                                                                                                                                                                                                                     | The mockup's own caption teaches a label the run removed. Every rendered `.rolelab` in the same file says `operator` (:188, :189, :191, :192), and both spines say `operator` (EXPERIENCE.md:125, :244; DESIGN.md:609). `you` was rejected because it labels one person's message "you" when a second person reads it. Same defect the run already fixed once in `key-transcript-states.html`'s glyph legend. Both mockup copies must change together to stay byte-identical.                                                                                                                                                                                            |
| DESIGN.md:497 — "Inside the transcript, only the status glyph is bold — the dock's controls sit at weight 500 (`{typography.control}`), the one place in the panel that does, because a control is not a fact. Otherwise: only the status glyph is bold (`{typography.glyph.fontWeight}`)."                                                                                                                                                                                         | "Inside the transcript, only the status glyph is bold (`{typography.glyph.fontWeight}`). The dock's controls sit at weight 500 (`{typography.control}`), the one place in the panel that does, because a control is not a fact."                                                                                                                                                                                                                         | A visible splice: "only the status glyph is bold" is stated twice in one sentence, and the second copy carries the token reference the first one dropped. Reordering states the rule once, then the exception, and loses nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| EXPERIENCE.md:96 — "The dock speaks in the same register as the transcript: lowercase, short, stating what will happen rather than reassuring."                                                                                                                                                                                                                                                                                                                                     | "The dock speaks in the same register as the transcript: short, stating what will happen rather than reassuring — lowercase everywhere except the four control labels."                                                                                                                                                                                                                                                                                  | The sentence is falsified by the table eight lines above it: :86 requires `Queue`, `Send now`, `Stop node`, `Stopping…`, and :87 requires `QUEUED · 2` / `WILL SEND · 2`. A builder following "lowercase" literally would ship `queue` and `stop node` against the Do column. The rest of the dock's microcopy (`sent`, `delivered`, `this tab only`, the stop disclosure) is lowercase, so the fix is a carve-out, not a reversal.                                                                                                                                                                                                                                      |
| EXPERIENCE.md:364-373 (Flow 4) — "Kevin has the `implement` node open and running. He is watching rows append…" — and `He`/`he`/`him`/`his`/`His` at :364, :365, :366, :367 (×2), :368 (×2), :369, :372 (×3), :373                                                                                                                                                                                                                                                                  | Replace every Kevin pronoun in Flow 4 with they/them/their: "Kevin has the `implement` node open and running. They are watching rows append…"; :365 "They type into the composer"; :366 "They remember a second thing"; :367 "They press `Stop node` … They wait about six seconds"; :368 "They had half-assumed"; :369 "They type the third"; :372 "they press `Send now` … after they committed to it"; :373 "Their words are never lost to an error." | Kevin is they/them in every flow that predates the delta — 8 references in Flow 1, 3 in Flow 2 — and `He` appears only in the new Flow 4 (13 references). Flow 3's 4 `They` are the unnamed developer. The pair should hold one pronoun for one protagonist, and the established one is they/them; `_bmad-output/project-context.md:3` declares `user_name: 'kevin'` and no pronouns, so nothing in the run licenses `He`. Two subject-verb agreements change with it (:364 "is watching" → "are watching").                                                                                                                                                             |
| DESIGN.md:480 — "That shipped button is still out there — the Legacy ask card's own Submit reads 3.1:1 today — which is a production finding this run surfaces and does not own; it is listed in Open Questions."                                                                                                                                                                                                                                                                   | "That shipped button is still out there — the Legacy ask card's own Submit reads 3.1:1 today — which is a production finding this run surfaces and does not own; it is recorded here and in `.memlog.md`, not carried as an open question of this spine."                                                                                                                                                                                                | The pointer dangles. Open Questions (:658-675) is `--node-prompt` only, and the Resolved-during-finalize list (:677-685) never mentions the ask card, so a builder who follows the reference finds nothing and cannot tell whether the finding was dropped or filed elsewhere. Revision states where the finding actually lives; if it is meant to become an Open Questions entry instead, add the entry rather than changing this line.                                                                                                                                                                                                                                 |
| DESIGN.md:499 — "Uppercase with tracking marks exactly two things: the occurrence header (`{typography.occurrence-header.letterSpacing}`) and a todo phase label (`{typography.phase-label.letterSpacing}`)."                                                                                                                                                                                                                                                                       | "Uppercase with tracking is reserved for labels and appears nowhere else: the occurrence header (`{typography.occurrence-header.letterSpacing}`), a todo phase label (`{typography.phase-label.letterSpacing}`), the `assistant` and `operator` role labels, and the draft box header."                                                                                                                                                                  | The delta falsified a sixth section that the rubric pass missed (it fixed the sans exception, the bold rule, the surface stack, Shapes, and Layout & Spacing). The dock adds two uppercase-with-tracking objects — the draft box header at :632 (`{typography.phase-label}` uppercase) and the `operator` role label at :609 — and the `assistant` role label at :606 was already a third. Keeping the restriction ("nowhere else") preserves the rule's purpose while making the enumeration true.                                                                                                                                                                      |
| EXPERIENCE.md:126 — Component Patterns, Message status row, Where column: "Operator row, label line"                                                                                                                                                                                                                                                                                                                                                                                | "Operator text, label line"                                                                                                                                                                                                                                                                                                                                                                                                                              | The pair names this component **Operator text** everywhere else — EXPERIENCE.md:125 (Component Patterns), :180 (State Patterns Surface column), :401; DESIGN.md:609. "Operator row" appears in no Components list in either file, so the one cell that points a builder from Message status to its host names a component that does not exist under that name. Running prose may keep saying "an operator row"; the Where column is a cross-reference and must use the component name.                                                                                                                                                                                   |
| EXPERIENCE.md:170 — "**`Stopping…` needs an end, not just a beginning.** The poll is a bounded wait, so past roughly two poll intervals the interface says so rather than spinning forever: the control returns to `Stop node` and the dock states that the stop did not land, which is honest and leaves the operator able to try again. Stop becomes `Stopping…` and goes `aria-disabled`; send stays `Queue`, because the node is still running and a message still only waits." | "Stop becomes `Stopping…` and goes `aria-disabled`; send stays `Queue`, because the node is still running and a message still only waits. **`Stopping…` also needs an end, not just a beginning**: past roughly two poll intervals the control returns to `Stop node` and the dock states that the stop did not land, so the operator can try again."                                                                                                    | The bounded-end fix was spliced in ahead of the state it qualifies, so the cell opens with the exception and reaches the rule in its third sentence. Reordered to rule-then-exception, the order every other State Patterns cell uses. Also drops "which is honest and" — the sentence already demonstrates the honesty; saying so is the reassurance this spine's voice bans. The `No progress bar` sentence that follows is unchanged.                                                                                                                                                                                                                                 |
| EXPERIENCE.md:246 — "**The control focus lands on has just changed what it does.** That same moved focus arrives on a control whose label flipped from `Queue` (holds a message, harmless) to `Send now` (delivers everything and resumes the node)."                                                                                                                                                                                                                               | "**The control that focus lands on has just changed what it does** — its label flipped from `Queue` (holds a message, harmless) to `Send now` (delivers everything and resumes the node)."                                                                                                                                                                                                                                                               | Two fixes in one sentence. "The control focus lands on" drops the relative pronoun and garden-paths as "the control focus"; adding "that" fixes it. And the second sentence restates the bolded lead before adding anything, so the two merge with no loss. Saves 11 words on a bullet whose payload (the focus ring measurement) is what a builder needs to reach.                                                                                                                                                                                                                                                                                                      |
| EXPERIENCE.md:245 — "**Focus must survive the stop, and it is dropped at the earlier of the two transitions.** There are two, and the dangerous one is not the obvious one. `running → stopping` is where it goes: the native `disabled` attribute blurs the element that carries it…"                                                                                                                                                                                              | "**Focus must survive the stop, and the transition that drops it is not the obvious one.** It goes at `running → stopping`: the native `disabled` attribute blurs the element that carries it…"                                                                                                                                                                                                                                                          | One fact stated three times in three consecutive sentences — the bolded lead, "There are two", and "`running → stopping` is where it goes". "Where it goes" also reads as a destination when it means a loss. Merged to one statement; the two-transitions structure survives because the bullet names `stopping → stopped` explicitly later.                                                                                                                                                                                                                                                                                                                            |
| EXPERIENCE.md:252 — "Two cells stay bold and both have a disposition there, neither of them silence."                                                                                                                                                                                                                                                                                                                                                                               | "Every bold dock cell has a disposition there, and none of them is silence."                                                                                                                                                                                                                                                                                                                                                                             | Disagrees with the table it points at. DESIGN.md:480 counts "The dock's **three** bold cells" — 1.5:1, 1.6:1 and 3.1:1 — while this line counts two, presumably counting rows. Dropping the count removes the drift without asserting a new number, and matches DESIGN.md:478's own phrasing ("Every bold cell has a disposition").                                                                                                                                                                                                                                                                                                                                      |
| DESIGN.md:553 — "The panel holds two halves: the transcript, which is read, and the **steering dock**, which it is written from. Everything from **Composer dock** down is the dock." Same construction at :3 — "the readable agent transcript that is read, and the steering dock it is written from".                                                                                                                                                                             | :553 → "The panel holds two halves: the transcript the reader reads, and the **Composer dock** they write from. Everything from **Composer dock** down is the dock." :3 → "the readable agent transcript the reader reads, and the composer dock they write from".                                                                                                                                                                                       | Two problems, two locations. The component is **Composer dock** in both files' Components lists; **steering dock** bolded beside it in the same sentence reads as a second object. (The table marker at DESIGN.md:464 is a section label, not a component reference, and can stay.) And "which it is written from" strands "it" on the panel two clauses back — EXPERIENCE.md:19 already solves the same sentence cleanly with "the half they write from".                                                                                                                                                                                                               |
| DESIGN.md:534 — "**The dock inverts the middle of that stack**: the dock itself is `surface-elevated` (it is chrome lifted off the transcript) while the composer field and draft box sink to `surface-inset` (they are wells written into). Same two tokens, used for the same reasons — lifted means chrome, sunk means content."                                                                                                                                                 | Consider: "**The dock uses the same two steps for the same reasons**: the dock itself is `surface-elevated` (chrome lifted off the transcript) while the composer field and draft box sink to `surface-inset` (wells written into) — lifted means chrome, sunk means content."                                                                                                                                                                           | The bullet contradicts its own closing clause: it opens "inverts" and closes "Same two tokens, used for the same reasons". A builder cannot tell whether the dock departs from the surface stack or restates it. Proposed as _Consider_ because the intended sense of "inverts" cannot be recovered from the text — if it means something real (a container elevated above content that is inset, where the transcript's containers rest on `surface`), say that instead of "inverts".                                                                                                                                                                                   |
| EXPERIENCE.md:36 — "…is the shipped HITL contract and does not change. **Steering adds exactly one thing to it, in exactly one place: a composer dock pinned to the bottom of the node panel.**"                                                                                                                                                                                                                                                                                    | Consider: "…is the shipped HITL contract, and **steering changes it in exactly one place: a composer dock pinned to the bottom of the node panel.**"                                                                                                                                                                                                                                                                                                     | Adjacent sentences assert that the chrome does not change and that steering adds to it. A builder reading the first clause alone gets the pre-delta rule. **Confirmation trail:** `.memlog.md` records this conflict being surfaced to the user, who chose "a one-sentence amendment rather than an override" — so the current shape is the user's answer and this is a rewording of it, not a reversal. The rest of the sentence (the `flex-wrap` header measurement and its reason) is untouched.                                                                                                                                                                      |
| EXPERIENCE.md:39 — "The two halves differ here, and the difference is worth stating in Foundation because a reader meets it as a claim about the panel. **The transcript half ships no schema, migration, or backend change**, so it improves every historical run the moment it deploys. **The steering half does not have that property**: it needs an engine seam…"                                                                                                              | "The two halves differ here. **The transcript half ships no schema, migration, or backend change**, so it improves every historical run the moment it deploys. **The steering half does not**: it needs an engine seam…"                                                                                                                                                                                                                                 | The opening clause explains why the paragraph sits in this section rather than telling the builder anything about the panel, and "does not have that property" spends four words on a pronoun. Saves 20 words; both bolded claims and the `engine-integration.md` citation survive verbatim.                                                                                                                                                                                                                                                                                                                                                                             |
| EXPERIENCE.md:176 — "…a steer carries the node id and retry epoch it was written against (`SPEC.md`), so a message composed here would be rejected on arrival."                                                                                                                                                                                                                                                                                                                     | "…a steer carries the node id and retry epoch it was written against (`spec-live-agent-steering/SPEC.md`), so a message composed here would be rejected on arrival."                                                                                                                                                                                                                                                                                     | Two specs are cited in this file's frontmatter and both have a `SPEC.md`, so the bare filename does not resolve. This is the same ambiguity the run already fixed for capability ids ("qualified by spec because both number their capabilities `CAP-n`", :326); the citation needs the same qualifier.                                                                                                                                                                                                                                                                                                                                                                  |
| EXPERIENCE.md:236 — "Steering adds four status channels to a panel whose whole guarantee is that status never rides on colour. Each is answered here."                                                                                                                                                                                                                                                                                                                              | Consider: "Steering adds status channels to a panel whose whole guarantee is that status never rides on colour. Each is answered here."                                                                                                                                                                                                                                                                                                                  | The four are never enumerated, and the very next bullet opens "**Four dock changes are status messages**" with a different four (sent, delivered, delivery failed, send blocked). Two unmatched fours in consecutive sentences read as one list a builder then fails to find. Dropping the count in the intro leaves the bullet's four as the only one.                                                                                                                                                                                                                                                                                                                  |
| EXPERIENCE.md:201 — "Both scopes are stated in the interface, because either one alone would mislead." Also :172 — "The draft and the typed text are preserved **and stated as preserved**".                                                                                                                                                                                                                                                                                        | Consider: name the strings, or scope the claims to what exists — :201 "The tab scope is stated in the interface (`this tab only`); the node scope rides the composer field's accessible name." :172 "…are preserved, and the dock says so."                                                                                                                                                                                                              | Both sentences require microcopy that Voice and Tone (:86-92) does not list and the mockup does not render — the only scope string anywhere is `this tab only`. A builder has to invent two strings and will not match the register. :241 says the composer field's accessible name states the node it writes to, which may be the intended per-node channel; if so, say it here. Proposed as _Consider_ because the fix depends on which channel the author meant, not on the wording.                                                                                                                                                                                  |
| EXPERIENCE.md:129 — "Bare `Send` is banned in Voice and Tone for hiding whether it queues or delivers, and it would hide it here too."                                                                                                                                                                                                                                                                                                                                              | "Bare `Send` is banned in Voice and Tone for hiding whether it queues or delivers."                                                                                                                                                                                                                                                                                                                                                                      | The trailing clause restates the clause before it with a pronoun. The ban and its reason are both kept — this is the sentence guarding the CAP-5 wording, so only the redundant tail goes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| EXPERIENCE.md:250 — "The dock is therefore inside whatever region wraps the node panel as a whole, not outside both — a write surface that belongs to no landmark is one a reader navigating by region will never be handed."                                                                                                                                                                                                                                                       | "The dock is therefore inside whatever region wraps the node panel as a whole, rather than in no region at all — a write surface that belongs to no landmark is one a reader navigating by region will never be handed."                                                                                                                                                                                                                                 | "not outside both" has no antecedent pair in the sentence; the reader has to reconstruct it from the previous bullet's scroller-and-landmark contrast. The replacement states the same exclusion directly. (The forward reference "the SC 2.4.11 answer below" is one bullet away and reads fine as "in the next bullet" if the section is ever reordered.)                                                                                                                                                                                                                                                                                                              |
| EXPERIENCE.md:180 — "Only claude echoes today." Same lowercase in mockups/key-steering-dock.html:199, "only claude echoes back the id we stamp".                                                                                                                                                                                                                                                                                                                                    | "Only Claude echoes today." / "only Claude echoes back the id we stamp"                                                                                                                                                                                                                                                                                                                                                                                  | Provider names are capitalised everywhere else in both spines — "Claude alone produces it" (:155), "Claude `TodoWrite`" (:119), "Codex cannot produce it" (:155), DESIGN.md:423. `claude` lowercase reads as a provider id, and the one genuine id in this material (`omp`, :369) is lowercase for that reason, so the distinction is load-bearing.                                                                                                                                                                                                                                                                                                                      |
| EXPERIENCE.md:248 — "**Target size**: the dock's own controls — stop, send, and the composer field — take a `{spacing.control-min-h}` 32px floor, clearing SC 2.5.8's 24px with room… **A draft item's keep and delete are the exception at 24×24**, grown on padding the way the Raw toggle is"                                                                                                                                                                                    | Consider: "**Target size**: the dock's own controls — stop, send, and the composer field — clear SC 2.5.8 at `{spacing.control-min-h}`, and a draft item's keep and delete are the exception at `{components.draft-item.controlMinSize}`, grown on padding the way the Raw toggle is. `DESIGN.md` carries both measurements and the reasoning."                                                                                                          | Reintroduces the defect the bullet 17 lines above it was rewritten to remove: :231 now points at `DESIGN.md` for the tool row's target size "which owns every measurement in this design", while this bullet writes 32px and 24×24 inline — the same numbers DESIGN.md:126 and :367 own. Proposed as _Consider_ because the numbers here also carry the 32-vs-24-vs-22 argument; if the argument needs them in place, the fix is to say so rather than to keep two copies silently.                                                                                                                                                                                      |
